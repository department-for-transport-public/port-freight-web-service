# port-freight-web-service

## Usage instructions
* Set up a mysql server or connect to an existing database 
* Create a local appsettings.Development.json file to override the DB connection strings

